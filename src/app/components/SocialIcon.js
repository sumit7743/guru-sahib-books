"use client"
import { Facebook, Twitter, Instagram, Youtube, User } from 'lucide-react';
const SocialIcon = () => (
    <div className="h-10 w-10 bg-gray-300 rounded-full hover:bg-white cursor-pointer transition-colors flex items-center justify-center">
        {/* Placeholder for actual icons */}
        <div className="w-5 h-5 bg-gray-400 rounded-full opacity-50"></div>
    </div>
);

export default SocialIcon;